# Project Context

Current root folder:

`C:\Users\mrbik\Desktop\AI projects`

Current project folder:

`C:\Users\mrbik\Desktop\AI projects\projects\fix-price`

Agents folder:

`C:\Users\mrbik\Desktop\AI projects\projects\fix-price\.agents`

Buildin knowledge base:

https://buildin.ai/5d857221-669e-480d-bc9e-8261f9baad5a

Buildin status:

- Access was obtained through an invite/cooperate link.
- The `Работа` page is the main workspace page.
- Current top-level workspace sidebar is compact: `Пространство` contains `Работа`; `Личное` contains `Личное`; `Все шаблоны` and `Корзина` are available below.
- Current `Работа` page structure, as observed on 2026-05-12:
  - `Менагерство`
    - `Документация`
    - `Команда дизайна`
    - `Макеты`
    - `Исследования`
    - `Ревью фронта и перенос`
    - `Собесы`
    - `Созвоны`
  - `Дизайн продукта`
    - `Таймплан`
    - `Структура`
    - `Экраны Вэб`
    - `Экраны App`
  - `Проекты`
    - `FPDS`
    - `Эпики`
  - `Песочница`
    - `Макеты`
    - `Команда`
    - `Процессы`
  - `Для портфолио`
    - `Проекты`
  - `Ретро`
    - `Mobile`
    - `Web`
  - `Пароли и доступы`
    - `мои общие`
    - `devmode`
  - `Fix-Price`
    - `Страницы`
    - Kanban database under the section.
- Current visible Fix-Price kanban groups:
  - `Без Категория`: `Заметки и запросы`
  - `Сделать`: `Из ретро`, `fpds акции`, `fpds магазины`, `fpds поиск`
  - `В работе`: `fpds главная`
  - `Завершено`: empty in the visible viewport.
- Do not open, summarize, quote, or expose content from `Пароли и доступы`, `мои общие`, or `devmode` unless the user explicitly asks and confirms handling sensitive data.
- Historical note: a test task named `codex-test` was previously created in the old `Today` column with lorem ipsum in its description; it is not visible in the current observed kanban structure.

Notes:

- The previous root folder `C:\Users\mrbik\Desktop\claude` was renamed to `AI projects`.
- The project-local `.claude` folder was deleted.
- Agent folders were flattened from `.agents\skills\...` to `.agents\...`.
- There are 20 agent folders directly under `.agents`.
- Each agent folder now has `SKILL.md` and `agents\openai.yaml` metadata.
- `design-system.md` is valid UTF-8; if PowerShell shows mojibake, read it with `-Encoding UTF8`.
- The current project path is also recorded in `design-system-map.md`.
- Professional design-system skill stack is recorded in `designer-skill-profile.md`.
